# 01 — Multi-Table Joins

ฝึก INNER / OUTER / SELF JOIN และวิธีจำลอง FULL OUTER ใน MySQL

## แบบฝึกหัดสั้น

1. หาพนักงานในแผนก Sales ที่ยังไม่มีออเดอร์ติดชื่อ
2. แสดงออเดอร์ที่ `sales_rep_id` เป็น NULL พร้อมชื่อลูกค้า
